public without sharing class SCIS_userTriggerHandlerClass {
    public static void updateUserLoginDetails(List<user> userList){
        list<Employee__c> employeeList = new list<Employee__c>();
        for(user usr : userList){
            Employee__c empolyee = new employee__C();
            
        }
    }
}